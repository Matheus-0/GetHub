import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    normal: {
        width: 150,
        height: 75,
        resizeMode: 'stretch',
        marginBottom: 10,
    },
    header: {
        width: 200,
        height: 100,
        resizeMode: 'stretch'
    },
});

export default styles;
