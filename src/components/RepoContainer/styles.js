import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    repoName: {
        fontSize: 18,
        fontWeight: 'bold',
    },
    gradientFlex: {
        flexDirection: 'row',
    },
    image: {
        borderRadius: 25,
        height: 50,
        width: 50,
    },
});

export default styles;
