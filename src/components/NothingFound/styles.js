import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flex: 1,
    },
    nothingFound: {
        color: 'white',
        fontSize: 30,
        marginBottom: 20,
    },
    description: {
        color: 'white',
        fontSize: 16,
        marginHorizontal: 15,
        textAlign: 'center',
    },
});

export default styles;
